cc_library_static {
    name: "libLLVM17TableGen",
    defaults: ["llvm17-defaults-no-generated-headers"],
    srcs: ["*.cpp"],
    target: {
        // llvm17-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm17-tblgen
        host: {
            enabled: true,
        },
    },
}
