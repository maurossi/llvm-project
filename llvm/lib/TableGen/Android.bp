cc_library_static {
    name: "libLLVM20TableGen",
    defaults: ["llvm20-defaults-no-generated-headers"],
    srcs: ["*.cpp"],
    target: {
        // llvm20-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm20-tblgen
        host: {
            enabled: true,
        },
    },
}
