cc_defaults {
    name: "llvm23-lib-defaults",
    vendor_available: true,
    defaults: ["llvm23-defaults"],
    export_header_lib_headers: ["llvm23-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
