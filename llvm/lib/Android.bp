cc_defaults {
    name: "llvm18-lib-defaults",
    vendor_available: true,
    defaults: ["llvm18-defaults"],
    export_header_lib_headers: ["llvm18-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
