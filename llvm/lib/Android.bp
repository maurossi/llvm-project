cc_defaults {
    name: "llvm17-lib-defaults",
    vendor_available: true,
    defaults: ["llvm17-defaults"],
    export_header_lib_headers: ["llvm17-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
