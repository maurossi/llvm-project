cc_library_static {
    name: "libLLVM14Support",
    vendor_available: true,
    defaults: ["llvm14-defaults-no-generated-headers"],
    target: {
        // llvm14-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm14-tblgen
        host: {
            enabled: true,
        },
        windows: {
            enabled: true,
        },
    },
    srcs: [
        "*.c",
        "*.cpp",
    ],
}
